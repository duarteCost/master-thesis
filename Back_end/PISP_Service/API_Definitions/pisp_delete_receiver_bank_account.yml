Delete receiver bank account by merchant
Delete receiver bank account by merchant

**Authorization is mandatory**
**Merchant Role is mandatory**

**To get athorization:**
  1 - [Login](https://localhost:5001/apidocs/#!/user/login)
  2 - Copy token from response
  3 - Click on authorize on this page
  4 - Paste the token into the text input
---
tags:
- PISP
security:
  - JWT: []
operationId: delete_receiver_bank_account
produces:
- application/json
- application/xml
responses:
  200:
    description: Successful operation
    schema:
      $ref: '#/definitions/success_delete_receiver_account'
  400:
    description: Missing or invalid credentials
    schema:
      $ref: '#/definitions/error40X'
  404:
    description: Bad request
    schema:
      $ref: '#/definitions/error40X'

definitions:

  success_delete_receiver_account:
    properties:
      response:
        type: string

  error40X:
    properties:
      response:
        type: string